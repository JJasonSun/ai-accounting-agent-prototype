# AI 记账 Agent 方案说明

**原型链接：** <https://jjasonsun.github.io/ai-accounting-agent-prototype/>

## 1. 最重要的设计决策

我把“处理项”定为风险分流和人工接管的最小单位，而不是整项记账任务。同一批资料中，正常项可以继续处理；金额冲突、低置信度和高金额项目单独交给人工。一张异常单据不会卡住整批任务，会计也能看清具体风险。页面会保留每次 AI 判断的原始资料、结果、置信度和依据。如果人工改了金额或科目，原有下游结果作废，Agent 从受影响的步骤重新执行。

## 2. 最不适合完全交给 AI 的步骤

我不会让 AI 直接入账，也不会让它独立处理高风险异常。凭证会影响企业账务和后续审计，最后责任仍然在人。所以 MVP 只生成凭证草稿，审核人员批准后才能导出，正式入账由会计在外部记账系统完成。金额冲突、匹配失败、疑似重复和单笔 5 万元及以上的项目，不看 AI 置信度，直接转人工。

## 3. MVP 保留的能力

如果只做一个 MVP，我会保留从上传资料到导出已批准凭证的这条主线。Agent 负责识别单据、匹配流水、推荐科目和生成草稿；系统把正常项和异常项分开，支持批量审核、人工修改后重跑，并记下每次操作。微信和邮箱自动收件、正式账簿直连、工资核算、税务申报、资金支付不进入首版。先看这套协作方式能不能减少重复处理，以及会计是否愿意依据页面上的证据审批结果。

## 4. AI 工具的使用与独立判断

这份作业不是让 AI 一次生成的。我先用 Claude Code（Claude Fable 5）做业务分析，生成 PRD 和原型方案初版；再用 Codex（GPT-5.6 Sol）对照题目检查遗漏，修正角色、状态和交互中不一致的地方，同时补齐正常路径和金额冲突路径。

产品判断由我完成。包括以处理项而不是整项任务分流，划分 Agent、记账会计和审核人员的职责，设定哪些情况必须人工接管，以及人工改动后如何重跑和再审核。前三个原型方向我都没有采用，因为它们没有让新人迅速看懂“现在发生了什么、为什么需要我、我该做什么”。AI 帮我加快了分析、实现和检查，最后的范围、规则和交互取舍由我决定。
