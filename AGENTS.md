# AI 记账 Agent — 项目规则

> 本文件只保留下次 Agent 不看到就容易做错的边界和工作流。详细产品机制以 `PRD.md` 为准，当前进度、页面验收和运行方式以 `README.md` 为准。

## 1. 项目定位与权威来源

这是产品经理实习线下作业：为财务外包公司的记账会计和审核人员设计一款 AI 记账 Agent，交付 PRD、可交互原型和一页方案说明。

| 内容 | 唯一权威来源 |
|---|---|
| 原始题目与评审要求 | `实习生线下作业.md`，不得改写 |
| 产品边界、状态、阈值、工具矩阵 | `PRD.md` |
| 当前状态、页面验收、运行方式、文件入口 | `README.md` |
| 一页提交说明 | `方案说明.md` |
| 可运行原型 | 根入口 `index.html` → `prototype/index.html` + `tokens.css` |

不要把 PRD 的完整状态表、异常表复制回本文件。业务规则变化时先更新 PRD，再同步 README、原型和方案说明中的受影响表述。

## 2. 当前实现

- 技术栈：原生 HTML、CSS、JavaScript；无构建步骤、无前端框架、无运行时外部依赖。
- 本地启动：在项目根运行 `python3 -m http.server 4174 --bind 127.0.0.1`。
- 预览地址：`http://127.0.0.1:4174/prototype/index.html`。
- `CLAUDE.md` 必须继续软链接到 `AGENTS.md`，两者不得分叉维护。
- 截至 2026-07-27：PRD 与方案说明正文已完成；原型已部署至 `https://jjasonsun.github.io/ai-accounting-agent-prototype/`，并在公开环境验收四组人工工作列表、正常审核/导出、金额冲突重跑、低置信度新草稿审核、高金额逐笔审核、单项移出批量范围、异常草稿增量导出和任务状态联动。
- 作业已提交；导出的实名 PDF 和 ZIP 不属于项目现役文件，不进入公开仓库。仓库与源码文件无需改名。

## 3. 产品行为红线

### Agent 边界

| 执行者 | 唯一职责 |
|---|---|
| DocumentAgent | 解析资料并输出结构化字段、字段置信度和来源引用 |
| MatchingAgent | 仅在当前账套内匹配交易，识别重复、缺失和冲突 |
| VoucherAgent | 推荐科目并生成凭证草稿，不执行正式入账 |
| Orchestrator | 拆解任务、调度、更新状态和维护审核队列，不修改业务字段 |

### 必须保持

1. 每项 AI 判断都展示：原始资料引用、判断结果、独立置信度、简短依据。
2. OCR、匹配和科目推荐置信度不取平均；处理项按关键判断中的最低置信度分流。
3. 默认阈值：`>=85%` 且无异常进入待批量审核，`60%–85%` 进入待确认，`<60%` 进入人工处理中。
4. 单笔金额 `>=50,000 元`、资料冲突、匹配失败、疑似重复和不可恢复的工具失败覆盖置信度规则，直接转人工。
5. VoucherAgent 只生成草稿。所有草稿必须人工批准后才能导出；正式账簿只能由会计在外部记账系统写入。
6. 人工修改必须记录前后值、理由和操作者；受影响步骤及下游结果失效，并从最早受影响步骤重跑。
7. 审计记录只可追加，不可删除；失败必须说明步骤和原因，不静默跳过、不猜测补全。
8. 所有检索、历史映射和规则均按账套隔离；Agent 不跨账套读取。
9. 工资核算、税务申报和资金支付属于人工专属，不进入 MVP。

详细状态转换、异常处理和工具调用失败去向只在 `PRD.md` 维护。

## 4. 原型设计约定

- 核心模型是“AI 连续执行，会计只在关键决策点介入；每次介入可解释、可撤销、可追溯”，不是聊天框，也不是通用财务后台。
- 页面采用任务上下文、业务步骤轨道、待处理队列、证据决策画布、固定动作栏和版本审计链。
- 面向新人时，中文业务动作必须是一级信息；`DocumentAgent` 等内部名称只作次级说明。
- 每个核心页面应让用户无需口头解释即可回答：我在处理谁的什么任务、当前到哪一步、为什么需要我、下一步是什么、操作会影响什么。
- 逻辑页面统一放在 `prototype/index.html`，共享 `tokens.css`；使用 hash 路由表达核心页面类型和异常处理状态，不创建相互复制的独立 HTML。
- 信息架构和交互逻辑优先于装饰，但页面仍需达到专业、克制、可扫描的成品质量。
- 原型工作使用 `web-design-engineer`；需要降低模板感时用 `hallmark` 作质量约束。不要使用 `huashu-design`。
- 不恢复或借鉴已删除的三版探索和旧 v0；当前金额冲突决策画布是视觉与交互基线。

## 5. 修改与验证纪律

- 先用作业要求和 PRD 判断需求，再改原型；不要为展示能力堆砌功能。
- 只修改当前目标需要的文件，匹配现有原生技术栈，不引入框架或投机性抽象。
- 原型文案不得虚构效率、准确率、客户数量或生产能力；演示数据必须能在 PRD 场景中找到依据。
- 任何交互改动都定义可验证结果；至少检查脚本语法、页面可访问性和受影响状态转换。
- 修改用户可见流程后，同步检查 `README.md`、`PRD.md` 和 `方案说明.md`，只更新真正受影响的权威面。

## 6. 最终交付门槛

- 评审者可从账套入口完成正常路径和金额冲突路径，并到达明确结束状态。
- PRD、原型和本规则使用相同的角色、阈值、队列名称和状态语义。
- 原型提供无需登录的公开 URL；`localhost` 只算本地验证，不算已部署。
- 方案说明保持一页以内，直接回答题目要求的四个问题。
- 不提交真实客户资料、凭据、密钥或未经验证的业务指标。
